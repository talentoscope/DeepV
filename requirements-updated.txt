# Suggested modernized dependency set. Test in a clean environment before upgrading.
# Core ML
torch>=2.0.0
torchvision>=0.15.0

# Numerical & imaging
numpy>=1.23
pillow>=9.0
scipy>=1.9
scikit-image>=0.19

# Rendering / cairo
pycairo>=1.21
cairocffi>=1.4

# Distance / geometry
chamferdist>=1.0.0  # verify compatibility; consider using PyTorch-native ops

# Utilities
tensorboard>=2.12
tensorboardx>=2.6
tqdm>=4.64
networkx>=2.8

# Optional: faster image augmentations
albumentations>=1.3

# Keep additional dev/test deps separate (pytest, flake8, etc.)
